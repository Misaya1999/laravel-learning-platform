@php($prefix = $announcement ? 'edit-'.$announcement->id : 'add')
<div class="form-group"><label for="{{ $prefix }}-title">Tiêu đề</label><input id="{{ $prefix }}-title" class="form-control" name="title" maxlength="160" required value="{{ old('title', $announcement?->title) }}"></div>
<div class="form-group"><label for="{{ $prefix }}-content">Nội dung</label><textarea id="{{ $prefix }}-content" class="form-control" name="content" rows="5" maxlength="2000" required>{{ old('content', $announcement?->content) }}</textarea><small class="text-muted">Tối đa 2.000 ký tự.</small></div>
<div class="form-row">
    <div class="form-group col-md-5"><label for="{{ $prefix }}-type">Loại thông báo</label><select id="{{ $prefix }}-type" class="form-control" name="type"><option value="info" @selected(old('type', $announcement?->type) === 'info')>Thông tin</option><option value="success" @selected(old('type', $announcement?->type) === 'success')>Tin mới</option><option value="warning" @selected(old('type', $announcement?->type) === 'warning')>Quan trọng</option></select></div>
    <div class="form-group col-md-7"><label for="{{ $prefix }}-published">Thời gian đăng</label><input id="{{ $prefix }}-published" type="datetime-local" class="form-control" name="published_at" value="{{ old('published_at', $announcement?->published_at?->timezone(config('app.display_timezone'))->format('Y-m-d\TH:i')) }}"><small class="text-muted">Giờ Việt Nam. Để trống để đăng ngay khi bật công khai.</small></div>
</div>
<div class="custom-control custom-switch"><input type="checkbox" class="custom-control-input" id="{{ $prefix }}-status" name="is_published" value="1" @checked(old('is_published', $announcement?->is_published ?? true))><label class="custom-control-label" for="{{ $prefix }}-status">Công khai trên website</label></div>
